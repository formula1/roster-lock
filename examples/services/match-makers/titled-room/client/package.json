{
  "name": "@roster-lock/titled-room-client",
  "version": "0.1.0",
  "private": true,
  "description": "titled-room's own matchmaker UI - meant to run inside a host shell's <iframe> (see core/match-agent/client), talking to the host over a postMessage MessageBridge rather than to match-agent directly",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@roster-lock/types": "workspace:*",
    "@roster-lock/utils": "workspace:*",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "react-router-dom": "^7.6.2"
  },
  "devDependencies": {
    "@types/react": "^19.1.2",
    "@types/react-dom": "^19.1.2",
    "@vitejs/plugin-react": "^4.4.1",
    "typescript": "^7.0.2",
    "vite": "^5.0.11"
  }
}
