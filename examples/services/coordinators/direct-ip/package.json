{
  "name": "@roster-lock/direct-ip-coordinator-service",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone game coordinator service for roster-lock's direct-tcp connection mode - registers as a relay-server Game Coordinator and runs the TCP rendezvous any direct-tcp game-runner plugin talks to. Not game-specific - reusable across game-runner plugins.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@roster-lock/direct-ip-coordinator": "workspace:*",
    "@roster-lock/types": "workspace:*",
    "@roster-lock/utils": "workspace:*",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "zod": "^4.3.5"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/node": "^20.11.0",
    "typescript": "^7.0.2"
  }
}
