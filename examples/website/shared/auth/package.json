{
  "name": "@roster-lock/website-auth",
  "version": "0.0.0",
  "private": true,
  "description": "Shared GitHub-OAuth-backed JWT auth for roster-lock website services",
  "license": "JSON",
  "author": "Sam Tobia",
  "type": "commonjs",
  "main": "dist/index.js",
  "types": "src/index.ts",
  "scripts": {
    "build": "rimraf ./dist && tsc",
    "prepare": "pnpm run build",
    "test": "vitest run"
  },
  "dependencies": {
    "@roster-lock/utils": "workspace:*",
    "hono": "^4.11.3"
  },
  "devDependencies": {
    "rimraf": "^6.1.3",
    "typescript": "^7.0.2",
    "vitest": "^4.1.5"
  }
}
