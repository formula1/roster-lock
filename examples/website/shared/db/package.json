{
  "name": "@roster-lock/website-db",
  "version": "0.0.0",
  "private": true,
  "description": "Shared D1 access for roster-lock website services - uploader identity and abuse (strikes/bans) state, common to every service on the shared roster-lock-website-db database",
  "license": "JSON",
  "author": "Sam Tobia",
  "type": "commonjs",
  "main": "dist/index.js",
  "types": "src/index.ts",
  "scripts": {
    "build": "rimraf ./dist && tsc",
    "prepare": "pnpm run build",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260103.0",
    "rimraf": "^6.1.3",
    "typescript": "^7.0.2",
    "vitest": "^4.1.5"
  }
}
