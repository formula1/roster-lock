{
  "name": "@roster-lock/untrusted-lua",
  "version": "1.0.0",
  "description": "Run lua in an untrusted environment",
  "license": "JSON",
  "author": "Sam Tobia",
  "type": "commonjs",
  "main": "dist/index.js",
  "types": "./src/index.ts",
  "roster-lock": {
    "pluginType": "untrusted-script"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf ./dist && tsc",
    "prepare": "npm run build"
  },
  "dependencies": {
    "@roster-lock/types": "file:../../core/types",
    "wasmoon": "^1.16.0"
  },
  "devDependencies": {
    "rimraf": "^6.1.3",
    "typescript": "^6.0.3"
  }
}
