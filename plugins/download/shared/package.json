{
  "name": "@roster-lock/dl-shared",
  "version": "1.0.0",
  "description": "Shared download utilities for roster-lock protocol plugins",
  "license": "JSON",
  "author": "Sam Tobia",
  "type": "commonjs",
  "main": "dist/index.js",
  "types": "./src/index.ts",
  "roster-lock": {
    "pluginType": "dl-shared"
  },
  "scripts": {
    "build": "rimraf ./dist && tsc",
    "prepare": "pnpm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@roster-lock/types": "workspace:*"
  },
  "devDependencies": {
    "@roster-lock/dl-archive-tar": "workspace:*",
    "@roster-lock/dl-compression-gzip": "workspace:*",
    "@types/node": "^25.9.1",
    "rimraf": "^6.1.3",
    "typescript": "^7.0.2"
  }
}
