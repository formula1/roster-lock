# Path Variables

The path variables were used to identify the individual assets associated to the piece.
When the piece was being added, the path variables were used.

Now that we don't expect the piece's files to change we don't need to change the path variables.
