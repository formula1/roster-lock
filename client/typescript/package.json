{
  "name": "@roster-lock/ts-client",
  "version": "1.0.0",
  "description": "Simple client to be used by javascript packages",
  "license": "JSON",
  "author": "Sam Tobia",
  "type": "commonjs",
  "main": "dist/index.js",
  "types": "src/index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf ./dist && tsc",
    "prepare": "npm run build"
  },
  "dependencies": {
    "@noble/ed25519": "^3.0.1",
    "@roster-lock/types": "file:../../core/types",
    "isomorphic-ws": "^5.0.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/ws": "^8.18.1",
    "rimraf": "^6.1.3",
    "typescript": "^6.0.2"
  }
}
